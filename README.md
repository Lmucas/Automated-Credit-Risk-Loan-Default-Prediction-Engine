# Automated-Credit-Risk-Loan-Default-Prediction-Engine
an end-to-end, fair, and explainable Machine Learning pipeline that predicts loan defaults while complying with regulatory standards (Fair Lending, ECOA). The system handles class imbalance, optimizes hyperparameters, generates model explainability artifacts, and exposes a RESTful API via FastAPI.
